require 'spec_helper'

describe Format2mp3::Wma2mp3 do
  context "when receive a .wma file should be convert to .mp3 file" do
    #let(:wma_file) { File.expand_path(File.join(__FILE__, '..', '..','files','wma.wma')) }
    it "does something" do
     # puts wma_file.inspect
    end
  end
end